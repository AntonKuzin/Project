@using Roles = ORM.Roles
@model IEnumerable<test.ViewModels.UserViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Зарегистрированные пользователи</h2>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Roles)
        </th>
        <th>
            Забанен?
        </th>
        <th>
            Действие:
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
       
        <td>
            @foreach (var temp in item.Roles)
            {
                <span>
                    @Html.DisplayFor(modelItem => temp.Name)
                </span>
            }
        </td>
        <td>
            @if (item.Roles.SingleOrDefault(u => u.Name == "banned") != null)
            {
                <span>Да</span>
            }
            else
            {
                <span>Нет</span>
            }
        </td>
        <td>
            @if (item.Roles.SingleOrDefault(u => u.Name == "banned") != null)
            {
                @Html.ActionLink("Разбанить!", "UnBan",new { id=item.Id })
            }
            else
            {
                @Html.ActionLink("Забанить!","Ban",new { id=item.Id })
            }
        </td>
    </tr>
}

</table>
